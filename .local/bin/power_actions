#!/bin/bash

choice=$(printf "%s\n" "Suspend" "Poweroff" | dmenu -l 3 -p "Select System action : ")

case $choice in 
    Suspend) st -e sudo systemctl suspend;;
    Poweroff) st -e sudo systemctl poweroff;;
esac
